#!/usr/bin/python3
def uppercase(str):
    string = uppercase
    for i in str:
        if(ord(i)>=65 and ord(i)<=90):
            print('{}'.format(string))
            print('{}'.format(isupper()))
